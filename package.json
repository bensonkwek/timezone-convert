{
  "name": "timezone_covert",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "test": "node tests/utils.test.js",
    "lint": "eslint . --ext .js",
    "web-ext-lint": "web-ext lint --source-dir .",
    "audit": "npm audit --audit-level=moderate"
  },
  "devDependencies": {
    "eslint": "^10.1.0",
    "web-ext": "^10.1.0"
  }
}
